<Page
    x:Class="Views.Tela2"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ListBox Name="listaID">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                
                <DataTemplate>
                    <Grid  Background="AliceBlue">

                        <Grid.RowDefinitions>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="50"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="200"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Name="idNome"
                                   Text="{Binding full_name}"                               
                                   FontSize="10"
                                   TextWrapping="Wrap"
                                           Grid.Column="0"
                                           Grid.Row="0"/>
                        
                        <TextBlock Name="idNomeReposito"
                                   Text="{Binding name}"
                                   FontSize="10"
                                   TextWrapping="Wrap"
                                   Grid.Column="0"
                                   Grid.Row="1"/>
                        
                        <TextBlock Name="idDescriReposito"
                                   Text="{Binding description}"
                                   FontSize="10"
                                   TextWrapping="Wrap"
                                   Grid.Column="0"
                                   Grid.Row="2"/>
                        
                        <TextBlock Name="idNumeroStars"
                                   Text="{Binding stargazers_count}"
                                   FontSize="10"
                                   TextWrapping="Wrap"
                                   Grid.Column="1"
                                   Grid.Row="0"/>
                        
                        <TextBlock Name="idNumeroForks"
                                   Text="{Binding forks}"
                                   FontSize="10"
                                   TextWrapping="Wrap"
                                   Grid.Column="1"
                                   Grid.Row="1"/>
                    </Grid>
                    
                </DataTemplate>
            </ListBox.ItemTemplate>

        </ListBox>
    </Grid>
</Page>
